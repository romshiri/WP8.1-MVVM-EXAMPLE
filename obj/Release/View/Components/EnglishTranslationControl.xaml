<UserControl
    x:Class="Romfix.View.Components.EnglishTranslationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Romfix.View.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <Grid Margin="10,10,10,0"  Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel Margin="5" VerticalAlignment="Center">
                    <StackPanel  Orientation="Horizontal">
                        <TextBlock  Style="{StaticResource InputWordStyle}" Text="Take"/>
                        <TextBlock VerticalAlignment="Bottom" Style="{StaticResource NounWordStyle}" Margin="15,0,0,3" Text="noun"/>
                    </StackPanel>
                    <TextBlock Style="{StaticResource BelowInputWordStyle}" Text="takes, take, man"/>
                </StackPanel>
                <Border Height="2" Background="{StaticResource WordSpiltterColor}" VerticalAlignment="Bottom" Grid.ColumnSpan="2" Margin="5,0"/>
            </Grid>
            <Grid Grid.Row="1">
                
            </Grid>
        </Grid>

    </Grid>
</UserControl>

